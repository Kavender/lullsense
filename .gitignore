# Internal planning — never ship in the OSS repo
plan-do-not-commit/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# OS
.DS_Store

# LullSense local state — never commit a child's profile/constraints/experiments.
# This is the default --state-dir; guard against testing inside the repo tree.
.lullsense/
**/.lullsense/
